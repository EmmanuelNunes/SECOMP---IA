version: '3.8'

services:
  career-api:
    build: .
    container_name: career-ai-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
